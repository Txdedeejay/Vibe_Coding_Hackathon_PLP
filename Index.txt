<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudyAI - Learning Platform</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.socket.io/4.5.0/socket.io.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f5f7fb;
            color: #333;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            background: linear-gradient(135deg, #6e8efb, #a777e3);
            color: white;
            padding: 20px 0;
            border-radius: 10px;
            margin-bottom: 30px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }

        .logo {
            font-size: 28px;
            font-weight: 700;
            display: flex;
            align-items: center;
        }

        .logo i {
            margin-right: 10px;
        }

        .user-info {
            display: flex;
            align-items: center;
        }

        .user-info img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            margin-right: 10px;
        }

        .main-content {
            display: grid;
            grid-template-columns: 1fr 3fr;
            gap: 20px;
        }

        .sidebar {
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        }

        .nav-item {
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
        }

        .nav-item.active {
            background-color: #6e8efb;
            color: white;
        }

        .nav-item:hover {
            background-color: #f0f3ff;
        }

        .nav-item i {
            margin-right: 10px;
            font-size: 18px;
        }

        .content-area {
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        }

        .section {
            display: none;
        }

        .section.active {
            display: block;
        }

        .section-title {
            font-size: 24px;
            margin-bottom: 20px;
            color: #444;
            display: flex;
            align-items: center;
        }

        .section-title i {
            margin-right: 10px;
            color: #6e8efb;
        }

        .upload-area {
            border: 2px dashed #6e8efb;
            border-radius: 10px;
            padding: 30px;
            text-align: center;
            margin-bottom: 20px;
            background-color: #f9faff;
            transition: all 0.3s ease;
        }

        .upload-area:hover {
            background-color: #f0f3ff;
        }

        .upload-area i {
            font-size: 48px;
            color: #6e8efb;
            margin-bottom: 15px;
        }

        .btn {
            background: linear-gradient(135deg, #6e8efb, #a777e3);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        }

        .documents-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .document-card {
            background-color: #f9faff;
            border-radius: 10px;
            padding: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }

        .document-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }

        .document-card h3 {
            font-size: 18px;
            margin-bottom: 10px;
            color: #444;
        }

        .document-card p {
            color: #777;
            font-size: 14px;
        }

        .flashcard {
            perspective: 1000px;
            margin-bottom: 20px;
        }

        .flashcard-inner {
            position: relative;
            width: 100%;
            height: 200px;
            transition: transform 0.6s;
            transform-style: preserve-3d;
        }

        .flashcard.flipped .flashcard-inner {
            transform: rotateY(180deg);
        }

        .flashcard-front, .flashcard-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 10px;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .flashcard-front {
            background: linear-gradient(135deg, #6e8efb, #a777e3);
            color: white;
            font-size: 18px;
            text-align: center;
        }

        .flashcard-back {
            background: linear-gradient(135deg, #a777e3, #6e8efb);
            color: white;
            transform: rotateY(180deg);
            text-align: center;
        }

        .exam-question {
            background-color: #f9faff;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
        }

        .options {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-top: 15px;
        }

        .option {
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .option:hover {
            background-color: #f0f3ff;
            border-color: #6e8efb;
        }

        .option.selected {
            background-color: #6e8efb;
            color: white;
            border-color: #6e8efb;
        }

        .chat-container {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 20px;
            height: 500px;
        }

        .peers-list {
            background-color: #f9faff;
            border-radius: 10px;
            padding: 15px;
            overflow-y: auto;
        }

        .peer {
            padding: 12px;
            margin-bottom: 10px;
            background-color: white;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .peer:hover {
            background-color: #f0f3ff;
        }

        .peer.active {
            background-color: #6e8efb;
            color: white;
        }

        .chat-area {
            display: flex;
            flex-direction: column;
            background-color: #f9faff;
            border-radius: 10px;
            overflow: hidden;
        }

        .chat-header {
            background: linear-gradient(135deg, #6e8efb, #a777e3);
            color: white;
            padding: 15px;
            text-align: center;
        }

        .chat-messages {
            flex: 1;
            padding: 15px;
            overflow-y: auto;
        }

        .message {
            margin-bottom: 15px;
            max-width: 80%;
        }

        .message.sent {
            margin-left: auto;
        }

        .message-content {
            padding: 10px 15px;
            border-radius: 18px;
        }

        .message.received .message-content {
            background-color: white;
            border-top-left-radius: 4px;
        }

        .message.sent .message-content {
            background: linear-gradient(135deg, #6e8efb, #a777e3);
            color: white;
            border-top-right-radius: 4px;
        }

        .chat-input {
            display: flex;
            padding: 15px;
            background-color: white;
        }

        .chat-input input {
            flex: 1;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 20px;
            margin-right: 10px;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 20px;
        }

        .loading i {
            font-size: 40px;
            color: #6e8efb;
            margin-bottom: 10px;
        }

        @media (max-width: 768px) {
            .main-content {
                grid-template-columns: 1fr;
            }
            
            .options {
                grid-template-columns: 1fr;
            }
            
            .chat-container {
                grid-template-columns: 1fr;
                height: auto;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <i class="fas fa-brain"></i> StudyAI
                </div>
                <div class="user-info">
                    <img src="https://ui-avatars.com/api/?name=User&background=6e8efb&color=fff" alt="User">
                    <span>User</span>
                </div>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="main-content">
            <div class="sidebar">
                <div class="nav-item active" data-target="upload">
                    <i class="fas fa-file-upload"></i> Upload Documents
                </div>
                <div class="nav-item" data-target="documents">
                    <i class="fas fa-file-alt"></i> My Documents
                </div>
                <div class="nav-item" data-target="flashcards">
                    <i class="fas fa-sticky-note"></i> Flashcards
                </div>
                <div class="nav-item" data-target="exams">
                    <i class="fas fa-tasks"></i> Practice Exams
                </div>
                <div class="nav-item" data-target="peers">
                    <i class="fas fa-users"></i> Peer Learning
                </div>
            </div>

            <div class="content-area">
                <!-- Upload Section -->
                <div class="section active" id="upload">
                    <h2 class="section-title"><i class="fas fa-file-upload"></i> Upload Study Documents</h2>
                    <div class="upload-area" id="dropZone">
                        <i class="fas fa-cloud-upload-alt"></i>
                        <h3>Drag & Drop your files here</h3>
                        <p>or</p>
                        <input type="file" id="fileInput" style="display: none;">
                        <button class="btn" onclick="document.getElementById('fileInput').click()">Browse Files</button>
                    </div>
                    <div id="uploadStatus"></div>
                </div>

                <!-- Documents Section -->
                <div class="section" id="documents">
                    <h2 class="section-title"><i class="fas fa-file-alt"></i> My Documents</h2>
                    <div class="documents-list" id="documentsList">
                        <!-- Documents will be populated here -->
                    </div>
                </div>

                <!-- Flashcards Section -->
                <div class="section" id="flashcards">
                    <h2 class="section-title"><i class="fas fa-sticky-note"></i> Flashcards</h2>
                    <div id="flashcardsContainer">
                        <p>Select documents and generate flashcards from the Documents section.</p>
                    </div>
                    <div class="loading" id="flashcardsLoading">
                        <i class="fas fa-spinner fa-spin"></i>
                        <p>Generating flashcards...</p>
                    </div>
                </div>

                <!-- Exams Section -->
                <div class="section" id="exams">
                    <h2 class="section-title"><i class="fas fa-tasks"></i> Practice Exams</h2>
                    <div id="examsContainer">
                        <p>Select documents and generate practice exams from the Documents section.</p>
                    </div>
                    <div class="loading" id="examsLoading">
                        <i class="fas fa-spinner fa-spin"></i>
                        <p>Generating exam questions...</p>
                    </div>
                </div>

                <!-- Peer Learning Section -->
                <div class="section" id="peers">
                    <h2 class="section-title"><i class="fas fa-users"></i> Peer Learning</h2>
                    <div class="chat-container">
                        <div class="peers-list" id="peersList">
                            <!-- Peers will be populated here -->
                        </div>
                        <div class="chat-area">
                            <div class="chat-header">
                                <h3 id="currentPeer">Select a peer to start chatting</h3>
                            </div>
                            <div class="chat-messages" id="chatMessages">
                                <!-- Messages will be populated here -->
                            </div>
                            <div class="chat-input">
                                <input type="text" id="messageInput" placeholder="Type your message..." disabled>
                                <button class="btn" id="sendMessageBtn" disabled><i class="fas fa-paper-plane"></i></button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // DOM elements
        const navItems = document.querySelectorAll('.nav-item');
        const sections = document.querySelectorAll('.section');
        const fileInput = document.getElementById('fileInput');
        const dropZone = document.getElementById('dropZone');
        const uploadStatus = document.getElementById('uploadStatus');
        const documentsList = document.getElementById('documentsList');
        const flashcardsContainer = document.getElementById('flashcardsContainer');
        const examsContainer = document.getElementById('examsContainer');
        const peersList = document.getElementById('peersList');
        const chatMessages = document.getElementById('chatMessages');
        const messageInput = document.getElementById('messageInput');
        const sendMessageBtn = document.getElementById('sendMessageBtn');
        const currentPeer = document.getElementById('currentPeer');
        const flashcardsLoading = document.getElementById('flashcardsLoading');
        const examsLoading = document.getElementById('examsLoading');

        // State variables
        let documents = [];
        let selectedDocuments = [];
        let currentChatPeer = null;
        let socket = null;

        // Initialize the application
        function init() {
            setupNavigation();
            setupFileUpload();
            loadDocuments();
            setupPeerLearning();
        }

        // Set up navigation between sections
        function setupNavigation() {
            navItems.forEach(item => {
                item.addEventListener('click', () => {
                    const target = item.getAttribute('data-target');
                    
                    // Update active nav item
                    navItems.forEach(nav => nav.classList.remove('active'));
                    item.classList.add('active');
                    
                    // Show corresponding section
                    sections.forEach(section => section.classList.remove('active'));
                    document.getElementById(target).classList.add('active');
                    
                    // Load content for the section if needed
                    if (target === 'documents') {
                        loadDocuments();
                    } else if (target === 'peers') {
                        loadPeers();
                    }
                });
            });
        }

        // Set up file upload functionality
        function setupFileUpload() {
            // Click to upload
            fileInput.addEventListener('change', handleFileUpload);
            
            // Drag and drop
            dropZone.addEventListener('dragover', (e) => {
                e.preventDefault();
                dropZone.style.backgroundColor = '#f0f3ff';
            });
            
            dropZone.addEventListener('dragleave', () => {
                dropZone.style.backgroundColor = '#f9faff';
            });
            
            dropZone.addEventListener('drop', (e) => {
                e.preventDefault();
                dropZone.style.backgroundColor = '#f9faff';
                
                if (e.dataTransfer.files.length) {
                    handleFiles(e.dataTransfer.files);
                }
            });
        }

        // Handle file upload
        function handleFileUpload(e) {
            handleFiles(e.target.files);
        }

        function handleFiles(files) {
            if (!files.length) return;
            
            const formData = new FormData();
            formData.append('file', files[0]);
            formData.append('user_id', 1); // In a real app, this would come from authentication
            
            uploadStatus.innerHTML = `<p>Uploading ${files[0].name}...</p>`;
            
            fetch('/upload', {
                method: 'POST',
                body: formData
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    uploadStatus.innerHTML = `<p style="color: green;">File uploaded successfully!</p>`;
                    loadDocuments(); // Refresh the documents list
                } else {
                    uploadStatus.innerHTML = `<p style="color: red;">Upload failed: ${data.message}</p>`;
                }
            })
            .catch(error => {
                uploadStatus.innerHTML = `<p style="color: red;">Upload error: ${error.message}</p>`;
            });
        }

        // Load user's documents
        function loadDocuments() {
            // In a real app, this would fetch from an API endpoint
            // For demo purposes, we'll use mock data
            documents = [
                { id: 1, filename: "Biology_Chapter_1.txt", uploaded_at: "2023-05-15" },
                { id: 2, filename: "Chemistry_Basics.pdf", uploaded_at: "2023-05-18" },
                { id: 3, filename: "Physics_Notes.txt", uploaded_at: "2023-05-20" }
            ];
            
            renderDocuments();
        }

        // Render documents list
        function renderDocuments() {
            documentsList.innerHTML = '';
            
            documents.forEach(doc => {
                const isSelected = selectedDocuments.includes(doc.id);
                
                const docElement = document.createElement('div');
                docElement.className = `document-card ${isSelected ? 'selected' : ''}`;
                docElement.innerHTML = `
                    <h3>${doc.filename}</h3>
                    <p>Uploaded: ${doc.uploaded_at}</p>
                    <div style="margin-top: 15px;">
                        <button class="btn" onclick="toggleDocument(${doc.id})" style="margin-right: 10px;">
                            ${isSelected ? 'Deselect' : 'Select'}
                        </button>
                        <button class="btn" onclick="generateFlashcards([${doc.id}])" style="background: linear-gradient(135deg, #4caf50, #2e7d32);">
                            Generate Flashcards
                        </button>
                        <button class="btn" onclick="generateExams([${doc.id}])" style="background: linear-gradient(135deg, #ff9800, #ef6c00); margin-top: 10px;">
                            Generate Exam
                        </button>
                    </div>
                `;
                
                documentsList.appendChild(docElement);
            });
            
            // Add option to generate from all selected documents
            if (selectedDocuments.length > 0) {
                const generateAllElement = document.createElement('div');
                generateAllElement.className = 'document-card';
                generateAllElement.innerHTML = `
                    <h3>Generate from ${selectedDocuments.length} selected documents</h3>
                    <div style="margin-top: 15px;">
                        <button class="btn" onclick="generateFlashcards(selectedDocuments)" style="background: linear-gradient(135deg, #4caf50, #2e7d32); margin-right: 10px;">
                            Generate Flashcards
                        </button>
                        <button class="btn" onclick="generateExams(selectedDocuments)" style="background: linear-gradient(135deg, #ff9800, #ef6c00);">
                            Generate Exam
                        </button>
                    </div>
                `;
                
                documentsList.appendChild(generateAllElement);
            }
        }

        // Toggle document selection
        function toggleDocument(docId) {
            if (selectedDocuments.includes(docId)) {
                selectedDocuments = selectedDocuments.filter(id => id !== docId);
            } else {
                selectedDocuments.push(docId);
            }
            
            renderDocuments();
        }

        // Generate flashcards from selected documents
        function generateFlashcards(docIds) {
            flashcardsLoading.style.display = 'block';
            flashcardsContainer.innerHTML = '';
            
            // In a real app, this would call the API
            // For demo, we'll use mock data after a delay
            setTimeout(() => {
                const mockFlashcards = [
                    { question: "What is the powerhouse of the cell?", answer: "Mitochondria" },
                    { question: "What process do plants use to make food?", answer: "Photosynthesis" },
                    { question: "What is the basic unit of life?", answer: "Cell" }
                ];
                
                renderFlashcards(mockFlashcards);
                flashcardsLoading.style.display = 'none';
                
                // Switch to flashcards section
                navItems.forEach(nav => nav.classList.remove('active'));
                document.querySelector('[data-target="flashcards"]').classList.add('active');
                sections.forEach(section => section.classList.remove('active'));
                document.getElementById('flashcards').classList.add('active');
            }, 2000);
        }

        // Render flashcards
        function renderFlashcards(flashcards) {
            flashcardsContainer.innerHTML = '';
            
            flashcards.forEach((card, index) => {
                const cardElement = document.createElement('div');
                cardElement.className = 'flashcard';
                cardElement.innerHTML = `
                    <div class="flashcard-inner">
                        <div class="flashcard-front">
                            <p>${card.question}</p>
                        </div>
                        <div class="flashcard-back">
                            <p>${card.answer}</p>
                        </div>
                    </div>
                `;
                
                cardElement.addEventListener('click', () => {
                    cardElement.classList.toggle('flipped');
                });
                
                flashcardsContainer.appendChild(cardElement);
            });
        }

        // Generate exams from selected documents
        function generateExams(docIds) {
            examsLoading.style.display = 'block';
            examsContainer.innerHTML = '';
            
            // In a real app, this would call the API
            // For demo, we'll use mock data after a delay
            setTimeout(() => {
                const mockExams = [
                    {
                        question: "What is the chemical symbol for gold?",
                        options: ["Au", "Ag", "Fe", "Go"],
                        answer: "Au"
                    },
                    {
                        question: "Which planet is known as the Red Planet?",
                        options: ["Venus", "Mars", "Jupiter", "Saturn"],
                        answer: "Mars"
                    }
                ];
                
                renderExams(mockExams);
                examsLoading.style.display = 'none';
                
                // Switch to exams section
                navItems.forEach(nav => nav.classList.remove('active'));
                document.querySelector('[data-target="exams"]').classList.add('active');
                sections.forEach(section => section.classList.remove('active'));
                document.getElementById('exams').classList.add('active');
            }, 2000);
        }

        // Render exams
        function renderExams(exams) {
            examsContainer.innerHTML = '';
            
            exams.forEach((exam, index) => {
                const examElement = document.createElement('div');
                examElement.className = 'exam-question';
                examElement.innerHTML = `
                    <h3>Question ${index + 1}: ${exam.question}</h3>
                    <div class="options">
                        ${exam.options.map(option => `
                            <div class="option" data-value="${option}">${option}</div>
                        `).join('')}
                    </div>
                `;
                
                const options = examElement.querySelectorAll('.option');
                options.forEach(option => {
                    option.addEventListener('click', () => {
                        options.forEach(opt => opt.classList.remove('selected'));
                        option.classList.add('selected');
                        
                        // Show correct answer after a delay
                        setTimeout(() => {
                            if (option.getAttribute('data-value') === exam.answer) {
                                option.style.background = 'linear-gradient(135deg, #4caf50, #2e7d32)';
                            } else {
                                option.style.background = 'linear-gradient(135deg, #f44336, #d32f2f)';
                                
                                // Highlight correct answer
                                options.forEach(opt => {
                                    if (opt.getAttribute('data-value') === exam.answer) {
                                        opt.style.background = 'linear-gradient(135deg, #4caf50, #2e7d32)';
                                        opt.classList.add('selected');
                                    }
                                });
                            }
                        }, 500);
                    });
                });
                
                examsContainer.appendChild(examElement);
            });
        }

        // Set up peer learning functionality
        function setupPeerLearning() {
            // Initialize Socket.io connection
            socket = io();
            
            // Set up message sending
            sendMessageBtn.addEventListener('click', sendMessage);
            messageInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') {
                    sendMessage();
                }
            });
            
            // Socket event handlers
            socket.on('connect', () => {
                console.log('Connected to server');
            });
            
            socket.on('receive_message', (data) => {
                addMessageToChat(data.sender, data.message, false);
            });
        }

        // Load peers list
        function loadPeers() {
            // In a real app, this would fetch from an API endpoint
            // For demo purposes, we'll use mock data
            const mockPeers = ['Alice', 'Bob', 'Charlie', 'Diana'];
            
            peersList.innerHTML = '';
            mockPeers.forEach(peer => {
                const peerElement = document.createElement('div');
                peerElement.className = 'peer';
                peerElement.textContent = peer;
                peerElement.addEventListener('click', () => {
                    // Set active peer
                    document.querySelectorAll('.peer').forEach(p => p.classList.remove('active'));
                    peerElement.classList.add('active');
                    
                    // Load chat with this peer
                    loadChat(peer);
                });
                
                peersList.appendChild(peerElement);
            });
        }

        // Load chat with a specific peer
        function loadChat(peer) {
            currentChatPeer = peer;
            currentPeer.textContent = `Chat with ${peer}`;
            messageInput.disabled = false;
            sendMessageBtn.disabled = false;
            
            // In a real app, this would fetch message history from the server
            chatMessages.innerHTML = '';
            
            // Join the room
            socket.emit('join', { username: 'User', peer: peer });
            
            // Add some mock messages for demo
            addMessageToChat(peer, "Hi there! How can I help you with your studies?", false);
            addMessageToChat('User', "I'm having trouble understanding photosynthesis.", true);
        }

        // Send a message
        function sendMessage() {
            const message = messageInput.value.trim();
            if (!message || !currentChatPeer) return;
            
            // Add message to chat UI immediately
            addMessageToChat('User', message, true);
            
            // Send message via Socket.io
            socket.emit('send_message', {
                sender: 'User',
                receiver: currentChatPeer,
                message: message
            });
            
            // Clear input
            messageInput.value = '';
        }

        // Add a message to the chat UI
        function addMessageToChat(sender, message, isSent) {
            const messageElement = document.createElement('div');
            messageElement.className = `message ${isSent ? 'sent' : 'received'}`;
            messageElement.innerHTML = `
                <div class="message-content">${message}</div>
            `;
            
            chatMessages.appendChild(messageElement);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        // Initialize the app when the DOM is loaded
        document.addEventListener('DOMContentLoaded', init);
    </script>
</body>
</html>